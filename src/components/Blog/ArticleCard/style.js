import { styled } from "styled-components";

export const ArticleCardStyled = styled.li`
    width: 376px;
    height: 380px;

    background-color: var(--grey-0);
    border: 1px solid var(--grey-2);

    div {
        height: 180px;

        padding: 15px 32px 24px 24px;

        p {
            margin: 16px 0 20px 0;
        }
    }
`