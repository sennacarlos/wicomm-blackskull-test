import { styled } from "styled-components";

export const ListProductStyled = styled.ul`
    height: 500px;
    display: flex;
    gap: 20px;

    overflow-x: hidden;
    overflow-y: hidden;
    scroll-behavior: smooth;
`